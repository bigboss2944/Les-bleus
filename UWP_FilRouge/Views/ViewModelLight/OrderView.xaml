<Page
    x:Class="UWP_FilRouge.Views.ViewModelLight.OrderViewModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_FilRouge.Views.ViewModelLight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding OrderViewModelInstance, Source={StaticResource Locator}}"
    >

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock HorizontalAlignment="Left" Grid.Row="1" Text="TextBlock" TextWrapping="Wrap" VerticalAlignment="Bottom" Margin="0,0,0,314" />

        
        <StackPanel>
            <TextBox x:Name="txtB1" Text="{Binding User.Quantity, Mode=TwoWay}"/>
            <TextBox x:Name="txtB2" Text="{Binding User.Customer, Mode=TwoWay}"/>
            <TextBlock x:Name="txtB3" Text="{Binding User.Seller, Mode=TwoWay}"/>
            <Button x:Name="btn1" Command="{Binding ButtonClick}" Content="{Binding ButtonContent, Mode=OneTime}"/>
        </StackPanel>

    </Grid>
</Page>
