<UserControl
    x:Class="UWP_FilRouge.Views.ViewModelLight.CreateUserUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_FilRouge.Views.ViewModelLight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid>
        
        

        <StackPanel Orientation="Vertical">
            <TextBox PlaceholderText="FirstName" Text="{Binding user.Firstname, Mode=TwoWay}"/>
            <TextBox PlaceholderText="LastName" Text="{Binding user.Lastname, Mode=TwoWay}"/>
            <Button x:Name="btn1" Command="{Binding ButtonClick}"/>

        </StackPanel>

    </Grid>
</UserControl>
